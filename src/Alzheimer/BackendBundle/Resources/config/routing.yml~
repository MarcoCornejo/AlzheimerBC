admin:
    pattern:  /admin
    defaults: { _controller: BackendBundle:Security:index }

_administracion:
    pattern: /admin/
    defaults: { _controller: FrameworkBundle:Redirect:redirect, route: administracion}

